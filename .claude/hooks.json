{
  "PostToolUse": [
    {
      "matcher": "Edit && .py",
      "hooks": [
        {
          "type": "command",
          "command": "cd ~/polymarket-arb && python -m pytest tests/test_mm_*.py tests/test_*skew*.py tests/test_*spread*.py tests/test_*obi*.py tests/test_pregame*.py tests/test_silent*.py tests/test_monitor*.py tests/test_inventory*.py tests/test_daily_scan.py tests/test_session_summary.py -q --tb=short 2>&1 | tail -5"
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": "Bash && git push",
      "hooks": [
        {
          "type": "command",
          "command": "cd ~/polymarket-arb && bash scripts/scan_secrets.sh"
        }
      ]
    }
  ],
  "Stop": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "command",
          "command": "grep -rn 'api_key\\|private_key\\|webhook' ~/polymarket-arb/src/ 2>/dev/null | grep -v 'os.environ\\|os.getenv\\|\\.env' | head -5 || true"
        }
      ]
    }
  ]
}
